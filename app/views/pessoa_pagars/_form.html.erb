<%= form_for(@pessoa_pagar) do |f| %>
  <% if @pessoa_pagar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pessoa_pagar.errors.count, "error") %> prohibited this pessoa_pagar from being saved:</h2>

      <ul>
      <% @pessoa_pagar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
